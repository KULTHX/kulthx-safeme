<!DOCTYPE html>
<html dir="rtl" lang="ar">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{title}} - KULTHX </title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="/css/style.css" rel="stylesheet">
    <style>
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            /* Removed background: linear-gradient(135deg, #667eea 0%, #764ba2 100%); */
            min-height: 100vh;
        }
        .gradient-text {
            background: linear-gradient(45deg, #f0f0f0, #cccccc); /* Light gray gradient */
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }
        .animated-bg {
            /* Removed background: linear-gradient(-45deg, #667eea, #764ba2, #667eea, #764ba2); */
            /* Removed background-size: 400% 400%; */
            /* Removed animation: gradient 15s ease infinite; */
        }
        @keyframes gradient {
            0% { background-position: 0% 50%; }
            50% { background-position: 100% 50%; }
            100% { background-position: 0% 50%; }
        }
        .glow-effect {
            box-shadow: 0 0 20px rgba(255, 255, 255, 0.1);
            transition: all 0.3s ease;
        }
        .glow-effect:hover {
            box-shadow: 0 0 30px rgba(255, 255, 255, 0.2);
            transform: translateY(-2px);
        }
        .floating {
            animation: floating 3s ease-in-out infinite;
        }
        @keyframes floating {
            0%, 100% { transform: translateY(0px); }
            50% { transform: translateY(-10px); }
        }
    </style>
</head>
<body class="animated-bg">
    <!-- Navigation -->
    <nav class="bg-gray-900/80 backdrop-blur-xl p-6 shadow-2xl border-b border-gray-700">
        <div class="flex justify-end items-center max-w-7xl mx-auto">
            <h1 class="text-4xl font-extrabold bg-gradient-to-r from-purple-400 via-blue-400 to-cyan-400 bg-clip-text text-transparent tracking-tight">🔒 KULTHX SAFEME</h1>
        </div>
    </nav>

    <!-- Main Content -->
    <main class="container mx-auto px-6 py-8">
        {{{body}}}
    </main>

    <!-- Footer -->
    <footer class="bg-white/10 backdrop-blur-md border-t border-white/20 mt-16">
        <div class="container mx-auto px-6 py-8">
            <div class="text-center">
                <p class="text-white/80 mb-2">© 2025 KULTHX SAFEME - منصة آمنة لحماية نصوص Roblox</p>
                <p class="text-white/60 text-sm">تم الإنشاء بواسطة KULTHX لمجتمع Roblox</p>
                <div class="flex justify-center items-center mt-4 space-x-4 space-x-reverse">
                    <span class="text-white/60">المستخدمون المتصلون:</span>
                    <span id="userCount" class="bg-gray-700 text-white px-3 py-1 rounded-full text-sm">0</span>
                </div>
            </div>
        </div>
    </footer>

    <!-- Socket.IO -->
    <script src="/socket.io/socket.io.js"></script>
    <script src="/js/main.js"></script>
</body>
</html>


